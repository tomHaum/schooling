import java.lang.*;

public class NewtonsMethod {
	// instance calculate trig values. variables - replace the example below
	// with your own
	public static void main(String[] args) {

		// get two double numbers
		double x = 0;
		double y = 1.516;

		// get the natural logarithm for x
		System.out.println("Math.sin(" + x + ")=" + Math.sin(x));

		// get the natural logarithm for y
		System.out.println("Math.sin(" + y + ")=" + Math.sin(y));
		log();
	}

	public static void log() {
		// instance variables - replace the example below with your own

		// get two double numbers
		double x = 100;
		double y = 2.718;

		// get the natural logarithm for x
		System.out.println("Math.log(" + x + ")=" + Math.log(x));

		// get the natural logarithm for y
		System.out.println("Math.log(" + y + ")=" + Math.log(y));
	}
}
